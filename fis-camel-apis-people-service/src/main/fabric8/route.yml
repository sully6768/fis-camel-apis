metadata:
  name: fis-camel-apis-people-service-route
spec:
  host: people-api.apps.redhatps.com
  to:
    kind: Service
    name: fis-camel-apis-people-se
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None
status:
  ingress:
    - host: people-api.apps.redhatps.com
      conditions:
        - type: Admitted
          status: 'True'
      wildcardPolicy: None